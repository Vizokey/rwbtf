This creates Ec2 servers with load balancers attatched. It is used with multiple subnets for blue green deployment. There is also a user data script to determine which color the website will display.
